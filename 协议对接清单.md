# 小智协议对接清单（Android 客户端版）

基于三个来源交叉核对：
1. `78/xiaozhi-esp32` 固件源码（`main/ota.cc`、`main/system_info.cc`）与 `docs/` 协议文档
2. `huangjunsen0406/py-xiaozhi` 成熟客户端实现（`src/activation/`、`src/protocols/`）
3. 2026-08-31 对官方线上服务 `api.tenclass.net` 的真实请求实测

**凡标注「实测」的条目都已验证过，可直接照抄；标注「固件」的来自固件源码。**

---

## 1. 服务端地址（实测）

| 用途 | 地址 |
|---|---|
| OTA 配置下发 | `https://api.tenclass.net/xiaozhi/ota/` |
| 激活确认 | `https://api.tenclass.net/xiaozhi/ota/activate` |
| WebSocket | `wss://api.tenclass.net/xiaozhi/v1/` |
| MQTT | `api.tenclass.net:8883`（TLS） |

自建服务端时只需替换 OTA 地址，其余由服务端下发。

---

## 2. 设备身份（实测）

四元组，**全部本地生成并持久化**，缺一或变更都会导致重新注册、丢失绑定：

| 字段 | 格式 | 生成方式 |
|---|---|---|
| Device-Id | `AA:BB:CC:DD:EE:FF` MAC 格式 | 随机生成，首字节取 `0x02`（避开广播/组播段） |
| Client-Id | UUID | `UUID.randomUUID()` |
| Serial-Number | `SN-XXXXXXXX-<mac去冒号>` | py-xiaozhi 格式：`SN-` + md5(mac) 前 8 位大写 + `-` + mac |
| hmac_key | 64 位十六进制 | 随机 32 字节转 hex |

Android 6+ 拿不到真实 MAC（已随机化），**必须自造**。

---

## 3. OTA 请求（实测）

### 3.1 请求头

```
Activation-Version: 2          ← 第三方客户端固定用 2（v1 不带序列号，无法激活）
Device-Id:         <mac>
Client-Id:         <uuid>
Serial-Number:     <SN-...>
User-Agent:        <boardType>/<appName>-<appVersion>
Accept-Language:   zh-CN
Content-Type:      application/json
```

**User-Agent 校验规则（实测踩坑 + 回归补充）**：

| UA | body 形态 | 结果 |
|---|---|---|
| `bread-compact-wifi/py-xiaozhi-2.1.1` | py-xiaozhi 风格（带 board 段） | 200 |
| `Xiaozhi/2.2.2 (esp32s3) IDF/v5.4.0` | 固件风格（无 board 段、无 SN） | **400 Invalid board type or app version** |
| `Xiaozhi/2.2.2 (esp32s3) IDF/v5.4.0` | py-xiaozhi 风格（带 board 段 + SN） | 200（服务端以 body.board 为准，UA 容错） |

规则（固件 `system_info.cc`）：`{BOARD_NAME}/{version}`。
py-xiaozhi 实际用的是 `{boardType}/{appName}-{appVersion}`，两种都能过。
**板型名用 `bread-compact-wifi`（py-xiaozhi 验证过的值），别自创。**
UA 强校验只在固件风格 body（无 board 段）路径触发——2026-08-31 端到端回归
`probe/e2e_regression.py` T2 验证。

### 3.2 请求体（实测）

```json
{
  "application": {
    "version": "2.1.1",
    "elf_sha256": "<hmac_key，服务端靠这个字段登记密钥>"
  },
  "board": {
    "type": "bread-compact-wifi",
    "name": "xiaozhi-android",
    "ip": "192.168.1.5",
    "mac": "<Device-Id>"
  }
}
```

固件发的是完整系统信息（flash_size / chip_info / partition_table...），第三方客户端
不需要 —— py-xiaozhi 只发上面这四个字段，实测通过。

### 3.3 响应（实测）

```json
{
  "server_time": {"timestamp": 1788185775581, "timezone_offset": 480},
  "firmware":  {"version": "...", "url": "..."},
  "mqtt":      {"endpoint": "...", "client_id": "...", "username": "...",
                "password": "...", "publish_topic": "...", "subscribe_topic": "..."},
  "websocket": {"url": "wss://api.tenclass.net/xiaozhi/v1/", "token": "..."},
  "activation":{"code": "997945", "message": "xiaozhi.me\n997945", "challenge": "<uuid>"}
}
```

**未绑定的三个特征（实测，用于诊断）**：

| 字段 | 未绑定值 | 含义 |
|---|---|---|
| `mqtt.client_id` 前缀 | `GID_test` | 测试组，非真实设备组 |
| `mqtt.subscribe_topic` | 字符串 `"null"` | 未分配订阅主题，收不到下行 |
| `websocket.token` | `"test-token"` | 占位符，WS 会话会被拒 |

出现任一特征 ⇒ 必须先走激活流程。

---

## 4. 激活流程（实测）

```
本地生成身份 ──► OTA（带 Serial-Number + elf_sha256=hmac_key）
                      │
                      ▼
          响应含 activation.code（6 位码）+ activation.challenge
                      │
                      ▼
          App 界面显示 6 位码，引导用户到 xiaozhi.me 登录输入
                      │
                      ▼  每 5 秒轮询
          POST /activate  {"Payload": {"algorithm":"hmac-sha256",
                                       "serial_number": ...,
                                       "challenge": ...,
                                       "hmac": HMAC-SHA256(hmac_key, challenge)}}
                      │
              ┌───────┴───────┐
            202              200
        等待用户输入       激活成功
                              │
                              ▼
                    重新 OTA 拿真实 token / subscribe_topic
```

**实测结果**：正确签名后服务端返回 `202 {"message":"Device activation timeout"}`，
说明 HMAC 已被接受、进入等待状态（错误签名会 401/400）。全程无需真实 eFuse 硬件。

**两个易错点**：

1. **载荷是 `{"Payload": {...}}` 嵌套结构**（py-xiaozhi 写法）。
   固件源码里是扁平的 `{"algorithm":...}`，那是 v1 无序列号路径（发 `"{}"`），别混。
2. **激活成功后必须重新调一次 OTA**，否则拿到的还是 `test-token`。

---

## 5. WebSocket 会话（固件 + 实测）

### 5.1 握手头（实测）

```
Authorization:    Bearer <OTA 返回的 token>
Protocol-Version: 1
Device-Id:        <mac>
Client-Id:        <uuid>
```

### 5.2 握手时序（固件 + py-xiaozhi 一致）

```
客户端 ──► 连接建立（101）
客户端 ──► {"type":"hello","version":1,
            "features":{"mcp":true},
            "transport":"websocket",
            "audio_params":{"format":"opus","sample_rate":16000,
                            "channels":1,"frame_duration":60}}
客户端 ◄── {"type":"hello","transport":"websocket",
            "session_id":"...","audio_params":{...}}    ← 10 秒内必须收到
```

**关键：101 是 nginx 层给的，不代表应用层接受。** 实测中带 `test-token` 也能拿到
101，但发完 hello 后立刻被 Close（code=0）。**以收到服务端 hello 为准**，
超时 10 秒判失败（与固件一致）。

### 5.3 消息类型（固件 docs/websocket.md）

| type | 方向 | 说明 |
|---|---|---|
| `hello` | 双向 | 握手协商 |
| `listen` | C→S | `state`: start/stop/**detect**；`mode`: auto/**manual**/realtime |
| `abort` | C→S | 打断 TTS，`reason`: wake_word_detected / user_interruption |
| `stt` | S→C | 语音识别结果 |
| `llm` | S→C | `emotion` 表情 + `text` |
| `tts` | S→C | `state`: start / **sentence_start**(带 text 字幕) / stop |
| `mcp` | 双向 | IoT 控制，JSON-RPC 2.0（**旧 `type:"iot"` 已废弃**） |
| `system` | S→C | 远程指令，当前仅 `reboot` |
| `alert` | S→C | 告警：status / message / emotion |

### 5.4 二进制帧（固件）

| 版本 | 结构 |
|---|---|
| v1（默认） | 裸 Opus，无封装 |
| v2 | `u16 version, u16 type, u32 reserved, u32 timestamp, u32 payload_size, payload[]`（服务端 AEC 用） |
| v3 | `u8 type, u8 reserved, u16 payload_size, payload[]` |

type：`0 = OPUS`，`1 = JSON`。

---

## 6. 音频参数（固件 + py-xiaozhi 一致）

| 方向 | 采样率 | 声道 | 帧长 | 编码 |
|---|---|---|---|---|
| 上行 | 16000 Hz | 1 | 60ms（960 采样） | Opus |
| 下行 | 可能 24000 Hz | 1 | 60ms | Opus |

- **下行要重采样**：解码后从 24k 转到设备输出采样率（48k/44.1k）再送 AudioTrack
- **帧长可选 20/40/60ms**（py-xiaozhi 按平台选，x86 用 20ms 低延迟，ARM 用 60ms 省电）
- **Android 采集用 `VOICE_COMMUNICATION` 音源**：让系统 AEC/AGC 生效，
  这是免 NDK 解决回声消除的关键

---

## 7. 切后台保活（Android 特有）

| 要求 | 说明 |
|---|---|
| 前台服务 | `foregroundServiceType="microphone"` |
| 权限 | `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_MICROPHONE`（Android 14+） |
| 通知 | 前台服务必须常驻通知 |
| 电池优化 | 引导用户加白名单，否则 Doze 会切断连接 |

---

## 8. 实测脚本与代码

| 产物 | 路径 |
|---|---|
| 官方服务器接入验证报告 | `probe/官方服务器接入验证报告.md` |
| 协议探测脚本（纯标准库） | `probe/xiaozhi_probe.py` |
| Android 工程骨架 | `xiaozhi-android/`（协议层已按本清单实现） |
| py-xiaozhi 参考源码摘录 | `py-xiaozhi-ref/` |

---

## 9. 风险提示

1. 官方从未公开承诺支持第三方客户端，现有放行是协议开放的自然结果
2. 服务端已在强校验 UA 格式（实测 400），随时可能加 MAC OUI / 频率等其他校验
3. 商业产品建议自建服务端（`xinnan-tech/xiaozhi-esp32-server` 最活跃），
   替换 `otaUrl` 一个参数即可切换
