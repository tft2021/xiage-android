# xiaozhi-android 重构与测试结果汇总

日期：2026-08-31
范围：core-protocol 重构收尾 + JVM 单元测试 + 真实服务器端到端回归

---

## 一、总体结论

**重构完成，两层测试全部通过：**

| 层级 | 用例数 | 结果 |
|---|---|---|
| JVM 单元测试（core-protocol，JUnit 5） | 51 | **51 通过 / 0 失败** |
| 端到端协议回归（真实服务器 api.tenclass.net） | 11 | **11 通过 / 0 失败** |

---

## 二、重构收尾内容

1. **`XiaozhiSession.kt` 类型引用修正**：`OtaClient.ActivateResult` / `OtaClient.OtaConfig`
   → 直接引用 `com.xiaozhi.protocol.ota` 包顶层类型（`ActivateResult` / `OtaConfig`）。
2. **依赖全部接口化**（可测试性核心）：
   - `OtaApi`（OTA + 激活，生产实现 `OtaClient`，测试注入 `FakeOtaApi`）
   - `XiaozhiTransport`（WS，生产实现 `XiaozhiWsClient`，测试注入 `FakeTransport`）
   - `AudioIO`（音频，生产实现 `AudioEngine`，测试注入 `FakeAudioIO`）
   - `AudioCodecProvider`（Opus 未接入时 `NoOpCodecProvider` 优雅降级）
3. **会话状态机下沉到 core-protocol**：`Idle → FetchingConfig → NeedActivation(code) →
   Connecting → Ready(sampleRate) → Listening/Speaking/Error`。
   激活轮询间隔与超时可注入，测试加速到毫秒级。
4. 修复会话层多个设计缺陷：下行采样率硬编码 24k（改为从服务端 hello 的
   `audio_params` 协商）、逐帧重建编解码器、先开采集后挂回调丢首帧等。

## 三、单元测试清单（51 个）

| 测试类 | 覆盖点 |
|---|---|
| `BinaryFrameCodecTest`（14） | v1 直通 / v2·v3 编解码往返 / 头部字节布局 / 截断帧与超长 size 判非法 / 未知版本 / v3 载荷超限 |
| `XiaozhiMessageParserTest`（15） | hello/stt/llm/tts/mcp/system/alert 解析、未知类型保留原文、设备侧消息 toJson 往返 |
| `DeviceCredentialsTest`（9） | HMAC-SHA256 对已知测试向量（`f7bc83f4...2d1a3cd8`）、64 位 hex 签名、SN 格式（独立 md5 交叉验证）、randomMacLike 首字节本地管理单播、持久化恢复 |
| `ResamplerTest`（8） | 同率拷贝、16k→32k 插值数值、3:2 / 2:3 长度、直流幅值不变、立体声分声道插值、非法参数 |
| `XiaozhiSessionTest`（7） | 已绑定直达 Ready；未绑定激活轮询（Waiting×2→Success→重新拉配置拿真实 token）；激活超时停在 NeedActivation；测试组凭据拒绝连接；listen 消息收发；TTS 驱动 Speaking/字幕；stop 释放资源 |

运行方式：`bash build-local.sh`（Microsoft JDK 17 + kotlin-compiler 2.0.21 离线编译，
junit-platform-console-standalone 执行，需 `--scan-class-path`）。

## 四、端到端回归（真实服务器）

脚本：`probe/e2e_regression.py`（纯标准库，每次运行生成全新设备身份）。

| 用例 | 结果 | 关键证据 |
|---|---|---|
| T1 OTA v2 新身份注册 | PASS | HTTP 200；下发激活码 `647568` + challenge；token=`test-token` |
| T2 UA 负面校验（固件风格 body） | PASS | 400 `Invalid board type or app version` |
| T3 激活端点（嵌套 Payload + HMAC） | PASS | HTTP 202 `Device activation timeout`（=等待输码，签名被接受） |
| T4 WS 握手（测试组凭据） | PASS | 101 后 Close(code=0)，10s 内无服务端 hello（应用层拒绝） |
| T5 二进制帧 v1/v2/v3 布局 | PASS | 与 Kotlin `BinaryFrameCodec` 布局交叉验证一致 |

### 新发现（协议清单已更新）

**服务端 UA 强校验有条件**：只在固件风格 body（无 `board` 段、无 `Serial-Number`）时触发；
带 `board` 段 + `Serial-Number` 的 py-xiaozhi 风格请求，服务端以 `body.board` 为准，UA 容错。
T2 首跑即暴露此差异（错误 UA + py-xiaozhi body 返回 200），修正断言条件后复现 400。

## 五、本轮修复的测试侧问题（记录备查）

1. 测试函数名含 `\/` → Kotlin 拒绝非法字符，改中文「比」表述。
2. `junit-platform-console-standalone execute` 需要 `--scan-class-path`，否则只打帮助。
3. `org.json` 的 `optBoolean("features.mcp")` 不支持点路径 → 改 `optJSONObject("features")`。
4. 测试正则用 `replace('-')` 连字符时误伤 `[0-9A-F]` 字符区间 → 直接写目标正则。

## 六、遗留事项

- **Opus 编解码未接入**（`NoOpCodecProvider` 降级中）——声音链路阻塞项，推荐先接 Concentus。
- app 模块仍需 Android Studio（Android SDK）才能出 APK；本机已可编译并测试 core-protocol 全部协议逻辑。
- 早期 `android/` 目录为旧工程，活跃工程是 `xiaozhi-android/`，后续可清理。
